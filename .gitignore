# Raw scrape cache. Tens of thousands of PDFs — never commit these.
cache/
node_modules/

# Generated site data is committed on purpose (GitHub Pages serves it),
# so docs/data/ is NOT ignored. See README "What gets committed".
*.log
.DS_Store
Thumbs.db

# Local reference lists of the CEO roll CSVs. Useful to keep, not to publish.
roll-csv-urls.txt
roll-csv-index.txt
roll-csv/

# QA output contains real elector names — keep it local.
qa-results.json
qa-full.log
qa-full-results.json
